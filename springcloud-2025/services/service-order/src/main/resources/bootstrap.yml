spring:
  application:
    name: service-order
  config:
    import:
      - nacos:service-order.properties?group=DEFAULT_GROUP
    activate:
      on-profile: dev
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848



server:
  port: 8000


